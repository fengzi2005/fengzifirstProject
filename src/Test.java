import javax.swing.*;
import net.miginfocom.swing.*;

public class Test {
    /*OperateInformation operateInformation = new DateBase();
        Result result = operateInformation.titleFindInfo("vedio");
        System.out.println(result.getCode());*/
    // 创建文本区域用于显示输出
        /*JTextArea textArea = new JTextArea(10, 30);
        textArea.setEditable(false); // 设置为只读
        JScrollPane scrollPane = new JScrollPane(textArea);*/
    //new TestJFrame();
    //new TestFindJFrame();
    //
    public static void main(String[] args) {
        /*OperateInformation operate = new DateBase();
        Result result = null;
        int index = 3;
        String str = "图书";
        switch (index) {
            case 1 :
                result = operate.idFindInfo(str);
                break;
            case 2:
                result = operate.titleFindInfo(str);
                break;
            case 3:
                result = operate.typeFindInfo(str);
                break;
        }
        System.out.println(result.getDate());*/
        new TestJF0();
        /*OperateInformation o =new DateBase();
        Result result = o.typeFindInfo("图书");
        System.out.println(result.getDate().toString());*/

    }
}


